# 앞서 만든 vocabulary.txt라는 파일에 단어들이 정리됨. 이 파일로 문제를 내 주는 프로그램을 만들려고 함.
# 한국어 뜻을 알려 주면, 사용자는 그에 맞는 영어 단어를 입력해야 함. 사용자가 입력한 영어 단어가 정답이면 "맞았습니다!"라고 출력하고, 틀리면 "아쉽습니다. 정답은 OOO입니다."가 출력되어야 함.
# 문제를 내는 순서는 vocabulary.txt에 정리된 순서.

with open('vocabulary.txt', 'r') as v: # 앞서 만든 파일을 읽으며 v에 저장하겠다.
    for line in v: 
        voc = line.strip().split(":") # 리스트로 정리하기 위해 화이트스페이스를 없애고 ":"를 기준으로 리스트를 출력해 voc에 저장하겠다.
        quiz = voc[1] # 우리가 내는 한국어. [cat: 고양이]라고 저장되어 있기 때문에 리스트 1번을 quiz에 저장.
        answer = voc[0] # 우리가 낸 한국어에 맞는 영어단어. 사용자의 입력값과 맞는지 대조하기 위해 리스트 0번을 answer에 저장.
        eng = input(f'{quiz}: ') # 사용자에게 문제를 내고 입력한 값을 eng에 저장.
        if eng == answer: # 사용자 입력값과 정답이 일치 한다면
            print('맞았습니다!') # 맞았습니다!가 출력
        else: # 둘이 일치하지 않는다면
            print(f'아쉽습니다. 정답은{answer}입니다.') # 아쉽습니다. 정답은 answer입니다. 하고 진짜 정답과 함께 출력.
