# 특수 문자 출력하기

print("|\\_/|")
print(r"|q p|   /}")
print("( 0 )\"\"\"\\")
print("|\"^\"`    |")
print("||_/=\\\\__|")

 - 파이썬 print문에서 따옴표는 코드의 일부로 인식하여 출력되지 않는다.
 - \(백슬래시) 뒤에 문자나 숫자가 오는 조합을 이스케이프 시퀀스(escape sequence)라고 하는데 백슬래쉬를 이용해 출력을 한다.
 - \\ 백슬래시
 - \' 작은 따옴표
 - \" 큰 따옴표
 - \b 백스페이스
 
 
 - raw string을 사용하면 escape 문 (ex)\b, \", \n..)이 동작되지 않고 있는 그대로 출력한다.
 - 출력할 문자열 앞에 r을 붙여준다.
